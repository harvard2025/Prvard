services:
  - type: web
    name: prvard
    env: python
    python_version: "3.10.9"
    build_command: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    start_command: gunicorn prvard.wsgi
